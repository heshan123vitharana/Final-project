{
  "name": "rice-mill-management-system",
  "version": "1.0.0",
  "description": "A comprehensive system for managing rice mill operations, including licensing, stock, and sales.",
  "main": "backend/server.js",
  "scripts": {
    "install": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "start": "node backend/server.js"
  },
  "keywords": [
    "react",
    "nodejs",
    "express",
    "mysql",
    "tailwindcss"
  ],
  "author": "Your Name",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
