{
  "name": "enhanced-paddy-management-dependencies",
  "version": "1.0.0",
  "description": "Additional dependencies for enhanced features",
  "dependencies": {
    "canvas": "^2.11.2",
    "nodemailer": "^6.9.7",
    "qrcode": "^1.5.3",
    "sharp": "^0.32.6"
  },
  "scripts": {
    "install-canvas-deps": "npm install canvas --build-from-source"
  },
  "notes": {
    "canvas": "Required for certificate generation with image manipulation",
    "nodemailer": "For sending temporary password emails",
    "qrcode": "Optional: for adding QR codes to certificates",
    "sharp": "Alternative image processing library (lighter than canvas)"
  },
  "installation_commands": [
    "cd backend",
    "npm install canvas nodemailer qrcode sharp",
    "mkdir -p assets/templates",
    "mkdir -p generated/certificates"
  ]
}